[project]
name = "python-booking-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=1.27.0",
    "starlette>=1.0.0",
    "uvicorn>=0.45.0",
]
